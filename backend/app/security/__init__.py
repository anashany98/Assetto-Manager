"""Security helpers (API keys/scopes, permissions, licensing).

These utilities are intentionally framework-agnostic so they can be reused
across HTTP and WebSocket entrypoints without creating router import cycles.
"""

